summary(combined_residuals[c("GDS_Total_Score", "DxCURREN")])

summary(remaining_subjects[c("GDS_Total_Score", "DxCURREN")])

table(combined_residuals$GDS_Total_Score)

table(combined_residuals$DxCURREN)
